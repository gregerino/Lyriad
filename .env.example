DATABASE_URL=
# Leave R2_* unset in local dev to fall back to local-disk storage (no cloud dependency).
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
# Optional, local storage driver only. Defaults to .local-storage at the repo root.
LOCAL_STORAGE_DIR=
AUTH_PASSWORD_HASH=
SESSION_SECRET=
